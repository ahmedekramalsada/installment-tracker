# Server port
PORT=3001

# Data directory for SQLite database (Docker uses /app/data)
DATA_DIR=./server

# JWT secret for authentication (change this in production!)
JWT_SECRET=your-secret-key-change-this

# Node environment
NODE_ENV=production
